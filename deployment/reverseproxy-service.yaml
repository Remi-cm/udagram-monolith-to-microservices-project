apiVersion: v1
kind: Service
metadata:
  name: reverseproxy
  labels:
    app: reverseproxy

spec:
  selector:
    app.kubernetes.io/name: reverseproxy
  ports:
    - protocol: TCP
      port: 8080
      name: "8080"
      targetPort: 8080